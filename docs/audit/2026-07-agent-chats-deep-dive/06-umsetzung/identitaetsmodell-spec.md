---
status: spezifikation-g0-g2
updated: 2026-07-20
description: Kanonischer Identitätsvertrag für capability-gegattete Claude-ID-Vergabe, per-Launch-Korrelation, atomare Claims und Laufzeit-Branchwechsel.
description_long: Definiert Weg B als Baseline, Weg A als live-probe-gegattete Capability, per-Launch Hook-Envelopes, den atomaren Claim-Vertrag, Recovery-Zustände sowie die vollständigen Übergänge für Branch, Rewind, Clear, Resume und Compact.
---

# Identitätsmodell für Claude-Session-Bindung

> **Status:** Revision nach G0–G2, Entscheidungen E1/E2/E5 eingearbeitet. Live-Probe (Paket B) blockiert — Capability bleibt fail-closed `hostAssignedUnsupported` (siehe [fork-hook-live-probe.md](../04-verifikation/fork-hook-live-probe.md)).

## Geltungsbereich und zentrale Regel

Diese Spezifikation betrifft normale interaktive Claude-Chats in echten PTYs; Agent Views haben keine eigene externe Session-ID, Background-Chats folgen beim Attach einem anderen Lifecycle, und Terminals besitzen überhaupt keine Claude-Identität (`WhisperM8/Services/AgentChats/AgentCommandBuilder.swift:282-321`).

Die verbindliche Identitätsregel lautet: **Resume behält die gescopte Provider-ID; Fork erzeugt eine neue gescopte Provider-ID. Weg B ist im Capability-Zustand `hostAssignedUnsupported` die Baseline: Claude vergibt Fresh-/Child-IDs und WhisperM8 bindet sie nachträglich mit strengen Guards. Weg A ist ausschließlich im Zustand `hostAssignedVerified` zulässig: WhisperM8 reserviert die Ziel-ID erst, nachdem eine isolierte Live-Probe der installierten CLI Fresh, Resume und Fork bestätigt hat.** Die beim Fork als `--resume` übergebene ID bleibt in beiden Zuständen nur Quell-/Eltern-ID; ein Binding entsteht ausschließlich über die unten definierte atomare Claim-API. (`docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:184-204`, `docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:303-308`)

## 1. Ist-Zustand: drei Identitäten in einer Workspace-Zeile

### 1.1 Workspace-/UI-Identität

`AgentChatSession.id` ist heute zugleich persistente Chat-/Workspace-ID, Tab-Schlüssel und Schlüssel für laufende Runtime-Subsysteme; dieselbe Struktur trägt außerdem `projectID`, `externalSessionID`, Launchmarker, Status und `forkSourceSessionID` (`WhisperM8/Models/AgentChat.swift:225-242`, `WhisperM8/Models/AgentChat.swift:281-303`). Die UI reicht diese lokale UUID unverändert an Terminal-Registry, Status-Coordinator, Hook-Vorbereitung und Hook-Tracking weiter (`WhisperM8/Views/AgentChatsView+Grid.swift:909-930`).

Ein Fork erhält zwar eine neue lokale `AgentChatSession.id`, startet aber nur mit `externalSessionID = nil` und `forkSourceSessionID = Eltern-ID`; persistierte `parentSessionID`, `rootSessionID`, autoritativer Transcriptpfad und ein Übergangszustand fehlen (`WhisperM8/Views/AgentChatsView+SessionLifecycle.swift:78-112`, `WhisperM8/Models/AgentChat.swift:281-303`).

### 1.2 Prozess-/PTY-Identität

Die Terminal-Registry besitzt pro lokaler Session-UUID höchstens einen laufenden Controller und verwendet damit die Workspace-ID als Prozess-Slot (`WhisperM8/Views/AgentTerminalView.swift:323-364`). Der Controller hat zwar eine eigene flüchtige Controller-UUID und kann die Shell-PID auslesen, diese Prozessinkarnation wird aber nicht als eigener persistenter Identitätsdatensatz geführt (`WhisperM8/Views/AgentTerminalView.swift:613-633`).

Auch Lifecycle-State, Hook-Liveness, Launch-Grace und Transcript-Watch sind ausschließlich mit der lokalen Session-UUID indiziert; ein Relaunch ersetzt damit die Prozessinstanz unter demselben fachlichen Schlüssel (`WhisperM8/Services/AgentChats/AgentSessionStatusCoordinator.swift:54-69`, `WhisperM8/Services/AgentChats/AgentSessionStatusCoordinator.swift:118-158`). Prozessende, Workspace-Chat und Claude-Zweig sind dadurch gekoppelt, obwohl ein Workspace-Chat mehrere aufeinanderfolgende PTY-Inkarnationen haben kann und ein PTY während `/branch` oder `/rewind` den Claude-Zweig wechseln kann (`docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:211-217`).

### 1.3 Claude-Session-/Branch-Identität

Die einzige aktive Claude-ID ist `AgentChatSession.externalSessionID`; `claudeProfileName` steht separat daneben, obwohl die fachliche Claude-Identität mindestens aus Config-Root und Session-ID besteht (`WhisperM8/Models/AgentChat.swift:225-239`, `WhisperM8/Models/AgentChat.swift:298-303`, `docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:218-220`). `transcript_path` und `cwd` werden aus Hook-Payloads geparst, beim Binding jedoch nicht persistiert (`WhisperM8/Services/AgentChats/ClaudeHookEventStore.swift:36-46`, `WhisperM8/Services/AgentChats/ClaudeHookEventStore.swift:121-136`, `WhisperM8/Services/AgentChats/AgentSessionStatusCoordinator.swift:345-363`).

Der Command-Builder bildet die Startabsicht bereits teilweise korrekt ab: Solange die Fork-Zeile keine eigene externe ID hat, baut er `--resume <forkSourceSessionID> --fork-session`; sobald `externalSessionID` gesetzt ist, baut er ein normales Resume auf diese ID (`WhisperM8/Services/AgentChats/AgentCommandBuilder.swift:337-385`). Der entscheidende Fehler liegt danach: Der Coordinator bindet beim `SessionStart` jede nichtleere Hook-ID ohne Parent-Guard und ohne Prüfung von Config-Root, Transcriptpfad, cwd oder Kollision mit einer anderen Workspace-Zeile (`WhisperM8/Services/AgentChats/AgentSessionStatusCoordinator.swift:200-214`, `WhisperM8/Services/AgentChats/AgentSessionStatusCoordinator.swift:345-363`).

Damit ist eine bestätigte WhisperM8-Fehlerkette beschrieben, **falls** ein früher Fork-`SessionStart` noch die Eltern-ID meldet: Die Eltern-ID würde als `externalSessionID` der Fork-Zeile gespeichert; der Indexer-Retry endet, weil er nur bei `externalSessionID == nil` weiterläuft; der nächste Launch verwendet die gespeicherte Eltern-ID ohne `--fork-session`. Ob die installierte Claude-CLI diese Ereignisfolge bei einem echten Fork tatsächlich emittiert, ist Gegenstand der noch ausstehenden isolierten Live-Probe in Paket B und wird hier nicht als deterministisch behauptet (`docs/audit/2026-07-agent-chats-deep-dive/06-umsetzung/README.md:58-67`, `WhisperM8/Views/AgentSessionDetailView.swift:700-725`, `WhisperM8/Services/AgentChats/AgentCommandBuilder.swift:337-385`).

Der Indexer kann die Lücke nicht zuverlässig heilen: Sein Live-Fallback wählt den zuletzt aktiven Kandidaten nach Provider, Projekt und einseitiger Zeituntergrenze, ohne bereits gebundene IDs auszuschließen oder Mehrdeutigkeit abzulehnen (`WhisperM8/Services/AgentChats/AgentSessionStore.swift:600-649`). Der Merge besitzt zwar ein echtes ±5-Sekunden-Fenster, ordnet bei mehreren ungebundenen Workspace-Zeilen aber den zeitlich nächsten Kandidaten zu und modelliert weder Fork-Lineage noch Prozessinkarnation (`WhisperM8/Services/AgentChats/AgentSessionStore.swift:825-887`). Der separate `ClaudeActiveSessionResolver` hat wiederum eine andere Kandidatenregel und ist nicht der produktive Lazy-Repair-Pfad (`WhisperM8/Services/AgentChats/ClaudeActiveSessionTracker.swift:21-60`, `docs/audit/2026-07-agent-chats-deep-dive/01-subsysteme/hooks-accounts.md:29-31`).

## 2. Soll-Modell

### 2.1 Drei getrennte Identitäten

| Identität | Lebensdauer und Schlüssel | Verantwortliche Daten | Darf nicht sein |
|---|---|---|---|
| **Workspace-Chat** | Langlebig; stabile lokale `chatID`/heutige `AgentChatSession.id`, unabhängig von Relaunch und Claude-Branch-Wechsel (`WhisperM8/Models/AgentChat.swift:225-242`). | UI-Metadaten, Projekt-Referenz, Parent-/Root-Chat-Referenz, aktive Binding-Referenz, Archiv-/Sortierzustand; der bestehende lokale Tab-Schlüssel bleibt erhalten (`WhisperM8/Services/AgentChats/AgentWindowStore.swift:105-183`). | Weder PID noch Claude-UUID als Primärschlüssel verwenden; ein Fork ist ein neuer Workspace-Chat und kein Überschreiben des Eltern-Chats (`docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:208-212`). |
| **Prozessinkarnation** | Flüchtig bis recovery-relevant; neue `launchID`/Generation für jeden PTY-Start unter einer `chatID` (`WhisperM8/Views/AgentTerminalView.swift:613-667`). | Controller-ID, PID und Startzeit, cwd-/Environment-Snapshot einschließlich Config-Root, Launch-Absicht (`fresh`, `resume(source)`, `fork(parent)`), Terminierungsgrund und Writer-Lease (`docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:250-264`). | Ein Relaunch darf alte Hook-/Indexer-Ergebnisse nicht als Ergebnis der neuen Inkarnation übernehmen; Prozessstatus darf nicht die Claude-Session-ID ersetzen (`docs/audit/2026-07-agent-chats-deep-dive/01-subsysteme/runtime-status.md:63-72`). |
| **Claude-Branch** | Langlebig und account-gescoped; Schlüssel `ProviderSessionKey(provider, canonicalConfigRoot, externalSessionID)` statt nackter UUID (`docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:218-220`). | `sessionID`, `parentSessionID`, `rootSessionID`, autoritativer `transcriptPath`, aktuelles cwd, letzte Dateigröße/MTime, Discovery-/Recovery-Zustand und optional bestätigende Event-/Record-ID (`docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:250-267`). | Projektpfad, Prozess-PID oder beim Launch übergebene Resume-ID sind keine ausreichende Branch-Identität; encoded cwd ist nur Discovery-Hinweis (`docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:214-220`). |

Der Workspace-Chat verweist über ein eigenes `activeClaudeBinding` auf genau einen bestätigten Claude-Branch; eine Prozessinkarnation verweist auf Chat und Launch-Absicht, nicht umgekehrt auf eine vermeintlich schon bekannte Ziel-ID (`docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:250-262`). Alle Hook-/Indexer-Entscheidungen müssen zusätzlich die `launchID` prüfen, damit verspätete Ereignisse einer alten Inkarnation kein aktuelles Binding überschreiben (`WhisperM8/Services/AgentChats/ClaudeHookBridge.swift:107-181`, `docs/audit/2026-07-agent-chats-deep-dive/01-subsysteme/hooks-accounts.md:121-126`).

### 2.2 Gemeinsamer normativer Vertrag G0–G2

Dieser Abschnitt ist wortgleich in `identitaetsmodell-spec.md` und `verlorene-chats-spec.md` zu führen. Bei Abweichungen ist dieses Dokument kanonisch; eine abweichende Implementierung ist nicht freigegeben.

#### Gescope Provider-Identität und Datenhoheit

Der kanonische Provider-Key lautet mindestens:

```text
ProviderSessionKey(provider, canonicalConfigRoot, externalSessionID)
```

Eine nackte Provider-UUID ist nie global eindeutig. `canonicalConfigRoot` wird aus dem beim Launch gespeicherten erwarteten Root gebildet: absolute standardisierte Pfadkomponenten, Auflösung vorhandener Symlinks und Entfernen redundanter `.`-/`..`-Komponenten. Ein Hook-`transcript_path` bestätigt den Root nur, wenn sein ebenfalls standardisierter und symlink-aufgelöster Pfad komponentenweise exakt unter `<canonicalConfigRoot>/projects/<encoded-cwd>/` liegt und auf `<externalSessionID>.jsonl` endet. Es wird nicht per String-Präfix verglichen. Passt der Pfad zu keinem oder zu mehreren bekannten Roots, lautet das Claim-Ergebnis `ambiguous`; der Root wird nie allein aus dem String des Transcriptpfads geraten. Bei noch nicht existierenden Pfadkomponenten wird der längste existierende Vorfahr aufgelöst und der Rest komponentenweise angehängt.

`~/.claude` und `~/.codex` sind für Identitäts-, Recovery- und Relink-Flows strikt read-only. Korrekturen, Aliase, Claims, Lineage und Recovery-Entscheidungen werden ausschließlich in WhisperM8-eigenen Daten persistiert. Die bestehenden Account-Umzugs- und Theme-Sync-Pfade sind keine allgemeine Ausnahme: Sie bleiben separat autorisierte, eng begrenzte User-Aktionen mit eigener Sicherung beziehungsweise eigenem Settings-Vertrag und dürfen von diesem Recovery-Vertrag nicht auf weitere Schreiboperationen in Provider-Daten ausgeweitet werden.

#### Capability-gegattete Launch-State-Machine

Der Capability-Zustand wird installationsbezogen in WhisperM8-eigenen Daten zusammen mit CLI-Pfad, CLI-Version, Probezeitpunkt und Probeergebnis persistiert. Unbekannt, veraltet, fehlgeschlagen oder per Rollback deaktiviert bedeutet `hostAssignedUnsupported`. Nur eine vollständig bestandene isolierte Live-Probe für Fresh, Resume **und** Fork darf `hostAssignedVerified` setzen. Ein Upgrade oder Wechsel des aufgelösten CLI-Binaries invalidiert die Verifikation.

| Capability-Zustand | Fall | Vor Spawn persistiert | Launch-Intent und CLI-Aufruf | Erwartete Provider-ID | Fehlerzustand |
|---|---|---|---|---|---|
| `hostAssignedUnsupported` | Fresh | `intent=fresh`, `expectedProviderKey=nil`, neue `launchID`/Generation, erwarteter Config-Root, cwd, `launchedAt` | Fresh ohne `--session-id`; Claude vergibt die ID | Genau ein neuer, ungeclaimter Key im erwarteten Root; Bindung erst durch aktuellen Hook oder schwächere eindeutige JSONL-Recovery | Kein oder mehr als ein Kandidat, belegter Key, Root-/Zeit-Mismatch oder Altgeneration → `recoveryRequired`; keine Row-Mutation |
| `hostAssignedUnsupported` | Resume | `intent=resume(sourceKey)`, `expectedProviderKey=sourceKey`, neue `launchID`/Generation, erwarteter Config-Root, cwd, `launchedAt` | `--resume <sourceKey.externalSessionID>` im Root des Source-Keys | Exakt `sourceKey` | Abweichende ID, falscher Root/Pfad oder Mehrdeutigkeit → `recoveryRequired`; kein stilles Rebind/Fresh |
| `hostAssignedUnsupported` | Fork | neuer lokaler Chat mit `intent=fork(parentKey)`, `expectedProviderKey=nil`, Parent-/Root-Lineage, neue `launchID`/Generation, Root, cwd, `launchedAt` | `--resume <parentKey.externalSessionID> --fork-session` ohne `--session-id` | Genau ein neuer, ungeclaimter Child-Key im Root des Parents; niemals `parentKey` | Parent-ID, kein/ein mehrfacher Child-Kandidat, Kollision, Root-/Zeit-Mismatch oder Altgeneration → `recoveryRequired`; keine Row-Mutation |
| `hostAssignedVerified` | Fresh | wie oben, zusätzlich vorab reservierter `expectedProviderKey=targetKey` | `--session-id <targetKey.externalSessionID>` im erwarteten Root | Exakt `targetKey` in Hook und Transcriptpfad | Mismatch/fehlende Bestätigung → `recoveryRequired`, Verifikation für dieses CLI-Binary deaktivieren; kein automatischer Retry als Fresh |
| `hostAssignedVerified` | Resume | `intent=resume(sourceKey)`, `expectedProviderKey=sourceKey`, neue `launchID`/Generation, Root, cwd, `launchedAt` | `--resume <sourceKey.externalSessionID>`; keine erneute Vorvergabe | Exakt `sourceKey` | Abweichung, Kollision, Root-/Pfad-Mismatch oder Altgeneration → `recoveryRequired`; keine Row-Mutation |
| `hostAssignedVerified` | Fork | neuer lokaler Chat mit `intent=fork(parentKey)`, vorab reserviertem `expectedProviderKey=childKey`, Parent-/Root-Lineage, neuer `launchID`/Generation, Root, cwd, `launchedAt` | Nur nach positiver Fork-Probe: `--session-id <childKey.externalSessionID> --resume <parentKey.externalSessionID> --fork-session` | Exakt `childKey`; Hook und Transcriptpfad müssen übereinstimmen | Parent-/andere ID, fehlende Datei, Kollision oder Altgeneration → `recoveryRequired`, Verifikation deaktivieren; Parent bleibt unverändert |

Die Launch-Transition lautet in beiden Capability-Zuständen `prepared → spawned → bindingPending → healthy | recoveryRequired`. `prepared` wird vor jedem Spawn synchron persistiert; `healthy` ist nur über `claimed` oder `alreadyOwnedBySameRow` erreichbar.

`hostAssignedVerified` ist damit kein zukünftiger Default, sondern ein reversibler, evidenzgebundener Capability-Zustand. Die Live-Probe aus Paket B entscheidet, ob er für die installierte CLI überhaupt erreichbar ist.

#### Per-Launch-Korrelation und Bridge-Envelope

Für jeden Spawn werden App-eigene, nicht wiederverwendete Pfade erzeugt:

```text
<WhisperM8-AppSupport>/ClaudeHooks/<chatID>/<launchID>/settings.json
<WhisperM8-AppSupport>/ClaudeHooks/<chatID>/<launchID>/events.jsonl
```

Vor Aktivierung einer neuen Generation muss die Bridge den FD/File-Watcher der vorherigen Generation schließen, deren Pfade als `superseded` markieren und die neue Generation samt Launch-Intent synchron persistieren. Erst danach werden Settings geschrieben und der PTY gestartet. Alte Dateien dürfen diagnostisch aufbewahrt werden, aber nie erneut aktive Eingangsquelle werden.

Jedes gelesene Provider-Ereignis wird vor der Auswertung in folgendes App-eigenes Envelope gelegt:

```text
BridgeEnvelope(
  chatID, launchID, generation, expectedConfigRoot, launchedAt,
  eventPath, observedAt, providerPayload
)
```

`SessionStart.source` wird aus dem Payload als typisierte Eingangsgröße `startup | resume | branch | rewind | clear | compact | unknown(rawValue)` geparst und zusammen mit dem Envelope weitergereicht; es darf nicht mehr verworfen werden. Nur `(chatID, launchID, generation)` der aktuell persistierten Generation darf claimen. Ein später Event einer alten Datei erhält Diagnose `staleGeneration` mit Pfad und beobachteter Provider-ID, verändert aber weder Binding, Recovery-State noch UI-Auswahl.

#### Atomare Claim-API

Hook, Indexer-Recovery und Control-CLI verwenden dieselbe Store-Operation unter demselben Workspace-Lock:

```text
claimProviderSession(
  chatID, launchID, generation, intent, candidateKey,
  transcriptPath, currentCwd, source, evidence
) -> ClaimOutcome
```

| `ClaimOutcome` | Bedeutung | Darf mutieren? |
|---|---|---|
| `claimed` | Kandidat erfüllt Generation, Intent, Root/Pfad, Zeitfenster, Erwartungs-ID und globale Eindeutigkeit | Ja: Key, Transcriptpfad, cwd, Source, Lineage und Recovery-State atomar committen |
| `alreadyOwnedBySameRow` | Derselbe kanonische Key gehört bereits derselben Row; das Event ist idempotent | Ja: ausschließlich neuere bestätigte Metadaten derselben Generation aktualisieren |
| `collision` | Der kanonische Key gehört einer anderen kanonischen Row oder Writer-Lease | Nein |
| `ambiguous` | Mehrere plausible Rows/Kandidaten/Roots oder widersprüchliche Evidenz | Nein |
| `staleGeneration` | `launchID`/Generation ist nicht mehr aktuell | Nein |

Nur `claimed` und `alreadyOwnedBySameRow` dürfen eine Row verändern. `collision` und `ambiguous` lassen Binding, Lineage, Transcriptpfad und sämtliche Chat-Row-Felder unverändert; der Aufrufer persistiert stattdessen einen separaten App-eigenen `RecoveryCase` und zeigt „prüfen“ an. `staleGeneration` bleibt reine Diagnose. Die User-Entscheidung im Recovery-Flow ruft anschließend dieselbe Claim-API mit einem explizit ausgewählten Kandidaten und neuer Revision auf, statt Felder direkt zu überschreiben.

Indexer-/JSONL-Evidenz ist gegenüber einem passenden aktuellen Hook schwächer. Sie darf keine Parent-Lineage erfinden: Nachrichtenfelder wie `parentUuid` sind ohne bestätigten Provider-Vertrag kein Branch-Parent-Beleg. Zulässig sind nur kanonischer Root, neue Dateiidentität, beidseitiges Fenster um `launchedAt`, cwd-Kompatibilität, gescopte Neuheit und globale Unbelegtheit. Diese Evidenz wird als `recoveryEvidence` persistiert und darf nur bei genau einem Kandidaten claimen; sonst folgt `ambiguous`.

#### Recovery-State-Machine

Recovery wird als eigener, App-eigener Sidecar `RecoveryCase(chatID, state, reason, candidateKeys, owningLaunchID, revision)` persistiert. Er verändert bei fehlgeschlagenen Claims keine Chat-Row. Unbekannte spätere Enum-Werte werden als `recoveryRequired(reason=unknownState)` erhalten, nicht verworfen. `healthy` bedeutet, dass kein aktiver RecoveryCase besteht; abgeschlossene Fälle dürfen als Diagnosehistorie erhalten bleiben.

| Zustand | Persistenz und zulässige Übergänge | UI-/Retry-Vertrag |
|---|---|---|
| `healthy` | kein aktiver `RecoveryCase`; bestätigtes Row-Binding bleibt unverändert. Vor Spawn entsteht ein separater LaunchRecord mit `bindingPending`; negativer Locator kann einen Case `missing` oder `worktreeDetached` erzeugen | normal sichtbar und startbar |
| `bindingPending` | im LaunchRecord mit aktueller `launchID`/Generation und Intent persistiert; nur `claimed`/`alreadyOwnedBySameRow` schließt den Case zu `healthy`, Unsicherheit/Crash/Timeout erzeugt `recoveryRequired` | sichtbar als „wird verbunden“; kein zweiter Fresh-Start |
| `recoveryRequired(reason, candidates)` | eigener Sidecar bleibt bis explizitem erfolgreichen Rescan/Relink/User-Claim bestehen; Archivieren der Chat-Row ändert den Case nicht | Sidebar und Archiv zeigen dauerhaft „prüfen“; Retry-Owner ist genau ein Recovery-Coordinator pro Chat, User wählt bei Mehrdeutigkeit |
| `missing(lastKnownKey, transcriptPath)` | separater Case nur nach bestätigtem Nichtfund; negative Scans löschen und verändern kein Row-Binding | sichtbar „Verlauf fehlt“; Rescan/Relink möglich, Launch gesperrt |
| `worktreeDetached(lastKnownKey, worktreeCwd)` | separater Case bei nicht auflösbarem Worktree/Basisrepo; Provider-History und Row bleiben unverändert | sichtbar „Worktree getrennt“; Ordnerwahl/Relink in App-Daten, kein Provider-Schreibzugriff |

„Neuen Chat bewusst anlegen“ erzeugt eine **neue lokale Row mit neuer Launchgeneration**; es setzt eine problematische Row nicht still auf Fresh zurück. Archivierte problematische Rows und ihre RecoveryCases bleiben auffindbar. Automatische Retries dürfen nur den RecoveryCase und seine Evidenzrevision aktualisieren, aber weder User-Auswahl simulieren noch einen mehrdeutigen Kandidaten claimen.

#### Chats-CLI-Revisionsvertrag

`whisperm8 chats new` darf sofort die lokale `chatID` als `--ref` zurückgeben. `wait --ref` hält jedoch keinen Workspace-Snapshot über mehrere Polls: Jeder Poll liest mindestens die aktuelle Workspace-Revision und löst die Row anhand der lokalen Referenz neu auf. Sobald Binding oder Recovery-State persistiert wird, erhöht der Single-Writer die Revision. Eine geänderte Revision erzwingt Re-Load von `externalSessionID`, `transcriptPath`, Generation und Recovery-State; erst die neu geladene Row wird an den Status-Probe übergeben. Datei-/Socket-Events dürfen Polls aufwecken, ersetzen aber nicht den Re-Load. So kann `new → wait --ref` ein nach dem Start eintreffendes Binding beobachten.

### 2.3 Vollständige Laufzeit-Übergangsmatrix

`activeBranchChange` ist eine Identitätstransition innerhalb derselben PTY-Inkarnation. Sie ist ausdrücklich verschieden von `inPlaceCompact`, das den Key nicht ändert, und von `processEnded`, das nur die Launchgeneration beendet. Die Row bleibt lokale UI-Identität; Provider-Branches werden separat über Binding-Historie auffindbar gehalten.

| Operation | Alter → neuer Provider-Key | `SessionStart.source` und Evidenz | Transcriptpfad / cwd / Lineage | Row, Tab, Unread, Auto-Naming | Negativ-/Mehrdeutigkeitsfall |
|---|---|---|---|---|---|
| `/branch` | `oldKey → newKey`, `newKey != oldKey`, gleicher Provider/Root | `branch` muss geparst sein und aus aktueller Generation kommen; neuer Key wird atomar geclaimt | neuer kanonischer Pfad passend zu `newKey`; cwd als Metadatum; `parentKey=oldKey`, `rootKey` geerbt | dieselbe lokale Row und derselbe Tab werden aktiv umgebunden; alter Key bleibt in Binding-Historie, kein Auto-Tab; Unread erst bei neuem Inhalt im nicht sichtbaren Tab; manueller Name bleibt, automatischer Name darf nach erstem neuen Inhalt einmal neu bewertet werden | fehlende Source, gleiche ID trotz Branch, mehrere Kandidaten oder Kollision → `recoveryRequired`; alter Key bleibt aktiv |
| `/rewind` | `oldKey → newKey`, `newKey != oldKey`, gleicher Provider/Root | `rewind` muss geparst sein; Branchpunkt ist nur aus autoritativem Hook/Provider-Feld oder expliziter User-Aktion ableitbar | neuer Pfad passend zu `newKey`; cwd aktualisierbar; `parentKey=oldKey` als erzeugende Branch-Lineage, optionaler Branchpunkt nur bei autoritativem Beleg | gleiche Row/Tab-Regeln wie `/branch`; kein Unread nur durch Transition; manuelle Namen bleiben | JSONL-Nachrichtenverkettung allein darf keinen Branchpunkt/Parent beweisen; fehlender eindeutiger neuer Key → `recoveryRequired` |
| `/clear` | `oldKey → oldKey` | `clear` wird geparst; Ereignis derselben Generation und exakt desselben Keys | derselbe kanonische Binding-Pfad; cwd darf aktualisiert werden; Lineage unverändert | `inPlaceClear`, gleiche Row/Tab; kein Transition-Unread; kein Auto-Rename, manuelle und automatische Titel bleiben | abweichende ID oder Root/Pfad → nicht als Clear akzeptieren, sondern `recoveryRequired`; kein Rebind |
| `/resume` | `oldKey → targetKey`; gleich bedeutet In-Place-Bestätigung, verschieden bedeutet `activeBranchChange` | `resume` wird geparst; `targetKey` muss explizit vom Provider-Ereignis derselben Generation und dem kanonischen Pfad bestätigt sein | Pfad/cwd des Targets; kein Parent-/Child-Verhältnis zwischen `oldKey` und `targetKey`, sondern Navigationshistorie | dieselbe Row/Tab wird auf Target umgebunden; alter Key bleibt auffindbar; Unread erst bei neuem Hintergrundinhalt; manueller Name bleibt, automatischer Titel darf nach erstem Target-Inhalt neu bewertet werden | nackte UUID ohne eindeutigen Root, belegter Target-Key oder mehrere Targets → `recoveryRequired`; alter Key bleibt aktiv |
| `/compact` | `oldKey → oldKey` | `compact` wird geparst; `SessionEnd(reason=compact)` plus folgender Start ist `inPlaceCompact`, kein Prozessende | Binding und Lineage unverändert; Pfad nur als Metadatenbestätigung desselben Keys, cwd aktualisierbar | gleiche Row/Tab; kein Unread und kein Auto-Rename durch Compact; Status darf kurz wechseln | abweichende ID, Root oder konkurrierende Generation → `recoveryRequired`; niemals `activeBranchChange` daraus raten |
| Prozessende/Crash | `oldKey → oldKey` | kein `SessionStart`; Terminierungsgrund schließt die aktuelle Generation | letzter bestätigter Pfad/cwd/Lineage bleiben erhalten | Row/Tab bleiben; kein Auto-Rename; laufender Status endet, Recovery richtet sich nach `bindingPending` vs. `healthy` | späte Events der geschlossenen Generation → `staleGeneration`; Crash in `bindingPending` → `recoveryRequired`, nicht Fresh |

Bei `/branch`, `/rewind` oder abweichendem `/resume` wird der bisherige Provider-Branch nicht gelöscht. Er bleibt als Branch-/Binding-Historie in WhisperM8-eigenen Daten auffindbar und kann bei explizitem Öffnen eine eigene Discovery-Row erhalten; die Transition selbst öffnet keinen zusätzlichen Tab. Auto-Naming arbeitet nie auf einem bloßen Transition-Event, sondern erst auf bestätigtem inhaltlichem Output des neuen Keys.

#### Testverträge `source`/Transitionen

Die folgenden IDs sind normative Oracle-Platzhalter; jeder Test kontrolliert zusätzlich, dass Binding, Lineage, Transcriptpfad und Chat-Row im Negativfall unverändert bleiben.

| Test-ID | Positiv-Oracle | Negativ-Oracle |
|---|---|---|
| `S-01` | Der Parser bildet `startup | resume | branch | rewind | clear | compact` jeweils auf den gleichnamigen typisierten Wert ab. | Jeder andere String bleibt verlustfrei als `unknown(rawValue)` erhalten; kein Default auf `startup`. |
| `S-02` | Ein Event der aktuellen `(chatID, launchID, generation)` erreicht Claim-/Transitionslogik. | Ein Event einer alten Generation liefert `staleGeneration`, erzeugt nur Diagnose und erreicht keine Row-Mutation. |
| `S-03` | `/branch` mit `source=branch`, eindeutigem neuem Key und passendem Pfad führt zu `activeBranchChange`. | Fehlende/falsche Source, gleicher Key, Kollision oder mehrere Kandidaten führt zum separaten `RecoveryCase`; alter Key bleibt aktiv. |
| `S-04` | `/rewind` mit `source=rewind` und eindeutigem neuem Key übernimmt nur autoritativ belegte Lineage. | Aus JSONL-Nachrichtenverkettung abgeleitete Lineage oder uneindeutiger neuer Key führt zu Recovery ohne Claim. |
| `S-05` | `/clear` mit `source=clear` bestätigt denselben Key als `inPlaceClear`. | Abweichender Key, Root oder Transcriptpfad wird nicht als Clear/Rebind akzeptiert. |
| `S-06` | `/resume` bestätigt exakt den expliziten Target-Key; bei abweichendem Target entsteht der definierte `activeBranchChange`. | Nackte UUID ohne eindeutigen Root, belegter Target-Key oder mehrere Targets führt zu Recovery; alter Key bleibt aktiv. |
| `S-07` | `/compact` mit `source=compact` und gleichem Key bleibt `inPlaceCompact`, ohne Unread oder Auto-Rename. | Abweichende ID, Root oder konkurrierende Generation wird weder als Compact noch als Branchwechsel geraten. |
| `S-08` | Reguläres Prozessende schließt ausschließlich die aktuelle Launchgeneration und erhält das letzte bestätigte Binding. | Spätes Event der geschlossenen Generation ist `staleGeneration`; Crash in `bindingPending` führt zu Recovery statt Fresh. |

Diese Oracles sind in der G4-Revision der `test-specs-welle0-1.md` als `A02-S01` bis `A02-S08` materialisiert; die alte A02-Direktbindung ist dort durch capability-/claim-spezifische Oracles ersetzt.

### 2.4 Verbindlicher Resume-Übergang

1. `planned`: Die Prozessinkarnation speichert `intent = resume(sourceKey)`; `sourceKey` ist der aktuell bestätigte `activeClaudeBinding`-Schlüssel, und vor dem Start bleiben Workspace-Chat und Branch-Binding unverändert (`WhisperM8/Services/AgentChats/AgentCommandBuilder.swift:349-385`, `docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:208-210`).
2. `spawned`: Der PTY startet mit exakt dieser ID und demselben Config-Root; der vorhandene Transcript-Guard und die Root-Selbstheilung bleiben vorgeschaltet (`WhisperM8/Services/AgentChats/AgentCommandBuilder.swift:362-405`, `WhisperM8/Views/AgentSessionDetailView.swift:537-610`).
3. `identityVerified`: Autoritativ ist ein Event derselben `launchID`, dessen `session_id == sourceKey.sessionID` ist und dessen Config-Root/`transcript_path` zum Source-Key passen; alternativ bestätigt eine validierte JSONL exakt denselben Schlüssel. Resume darf **keine andere ID heuristisch zum normalen Ergebnis erklären**, weil der offizielle Vertrag dieselbe ID verlangt (`docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:102-111`, `docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:184-204`).
4. `committed`: Es ändert sich nur die Prozess-/Lifecycle-Bindung; `activeClaudeBinding` bleibt derselbe Branch. Eine abweichende ID ist ein expliziter Branch-Wechsel oder Fehlerfall und muss separat verifiziert werden, statt als Resume-Ersatzkandidat still gebunden zu werden (`docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:211-218`, `WhisperM8/Services/AgentChats/AgentSessionStore.swift:653-715`).

### 2.5 Verbindlicher Fork-Übergang

1. `prepared`: Ein neuer Workspace-Chat wird mit eigener `chatID`, `parentChatID`, `rootChatID` und `intent = fork(parentKey)` persistiert; `activeClaudeBinding` bleibt bis zur Bestätigung leer. Der bestehende separate Fork-Tab und das Erben von Farbe/Account/Backend bleiben erhalten (`WhisperM8/Views/AgentChatsView+SessionLifecycle.swift:78-112`).
2. `spawned`: Im Zustand `hostAssignedUnsupported` startet der PTY ohne vorgegebene `--session-id` als `--resume <parentKey.sessionID> --fork-session`. Im Zustand `hostAssignedVerified` ist ausschließlich die in der Capability-Tabelle definierte, durch Paket B zu bestätigende Flagfolge mit vorab reservierter Child-ID zulässig (`WhisperM8/Services/AgentChats/AgentCommandBuilder.swift:337-385`, `WhisperM8/Models/AgentChat.swift:281-303`).
3. `identityVerified`: Ein frühes Event mit `session_id == parentKey.sessionID` darf den Fork-Chat **nicht** binden. Autoritativ ist ein zur aktuellen `launchID` gehörendes Ereignis mit dem erwarteten beziehungsweise eindeutig neuen Child-Key; übernommen werden `session_id`, `transcript_path` und cwd gemeinsam. Bei stummen/defekten Hooks darf ausschließlich eine neue, ungebundene JSONL desselben kanonischen Config-Roots als **schwächere Recovery-Evidenz** dienen, wenn Dateiidentität, beidseitiges Launchfenster, cwd, gescopte Neuheit und globale Unbelegtheit genau einen Kandidaten ergeben. Eine Parent-Lineage darf der JSONL-Fallback nicht aus `parentUuid` oder Nachrichtenverkettung erfinden (`docs/audit/2026-07-agent-chats-deep-dive/06-umsetzung/README.md:27-35`, `docs/audit/2026-07-agent-chats-deep-dive/06-umsetzung/README.md:76-94`, `docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:200-204`).
4. `committed`: In **einer** Store-Mutation werden Child-Key, Parent-/Root-Key, Transcriptpfad, cwd, Config-Root und Übergangszustand gespeichert; erst danach wird das Terminal/der Watcher auf die Child-ID umgehängt und crash-sicher geflusht. Der Eltern-Chat und sein Binding bleiben unverändert und auffindbar (`docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:208-212`, `WhisperM8/Services/AgentChats/AgentSessionStatusCoordinator.swift:345-363`).
5. Ein späterer Resume des Fork-Chats nimmt ausschließlich den bestätigten Child-Key; `forkSourceSessionID`/Parent-Key bleibt Lineage und darf nie wieder zum Resume-Fallback werden (`WhisperM8/Services/AgentChats/AgentCommandBuilder.swift:337-385`, `docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:258-262`).

## 3. Edge-Case-Matrix

| Fall | Ist-Verhalten | Soll-Verhalten laut Vertrag | Lücke | Fix-Skizze |
|---|---|---|---|---|
| **Resume** | Der Builder resumiert die gespeicherte `externalSessionID`; fehlt deren Transcript, kann `repairResumeStateBeforeLaunch` auf genau einen zeitlich plausiblen Projektkandidaten umbiegen, andernfalls stoppt der Launch sichtbar (`WhisperM8/Services/AgentChats/AgentCommandBuilder.swift:349-385`, `WhisperM8/Services/AgentChats/AgentSessionStore.swift:653-715`, `WhisperM8/Services/AgentChats/AgentSessionStore.swift:1189-1218`). | Resume schreibt dieselbe ID fort; die bestätigte Ziel-ID muss der Source-ID entsprechen (`docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:184-204`, `docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:208-210`). | Geplanter Source-Key, Prozessinkarnation und bestätigtes Ergebnis sind nicht getrennt; eine andere Session kann als „Repair“ übernommen werden (`WhisperM8/Services/AgentChats/AgentSessionStore.swift:1189-1218`). | Resume-Transition mit `launchID` einführen; nur dieselbe gescopte ID committen. Eine abweichende ID als Branch-Wechsel/Fehler anzeigen und nie als stilles Resume-Rebind behandeln (`docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:258-269`). |
| **Fork** | Neue lokale Zeile plus `forkSourceSessionID`; der Builder setzt die korrekten Flags, aber der erste `SessionStart` bindet jede gemeldete ID. Nach Eltern-ID-Bindung stoppt der Indexer-Retry, und späteres Resume öffnet den Parent (`WhisperM8/Views/AgentChatsView+SessionLifecycle.swift:78-112`, `WhisperM8/Services/AgentChats/AgentCommandBuilder.swift:337-385`, `WhisperM8/Services/AgentChats/AgentSessionStatusCoordinator.swift:200-214`, `WhisperM8/Views/AgentSessionDetailView.swift:700-725`). | Fork erzeugt eine neue ID und lässt den Parent unverändert; Child-ID, Transcriptpfad und Lineage werden nach dem autoritativen laufenden Ereignis atomar übernommen (`docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:184-204`, `docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:208-212`). | Kein Fork-Übergangszustand, kein Parent-ID-Guard, keine atomare Child-/Lineage-/Pfad-Bindung (`WhisperM8/Models/AgentChat.swift:225-303`, `WhisperM8/Services/AgentChats/AgentSessionStatusCoordinator.swift:345-363`). | `prepared → spawned → identityVerified → committed`; Parent-ID während Fork ignorieren, Child-Key plus Pfad/cwd/Lineage in einer Mutation committen, Fallback nur eindeutig und ungebunden (`docs/audit/2026-07-agent-chats-deep-dive/06-umsetzung/README.md:76-94`). |
| **`/compact`** | `SessionEnd(reason: compact)` gilt als In-Place-Restart; der Prozess bleibt aktiv, und ein folgender `SessionStart` bindet erneut dessen ID (`WhisperM8/Services/AgentChats/AgentSessionStateMachine.swift:222-235`, `WhisperM8/Services/AgentChats/AgentSessionStateMachine.swift:275-278`, `WhisperM8/Services/AgentChats/AgentSessionStatusCoordinator.swift:200-214`). | Compact bleibt dieselbe Session; weniger rekonstruierte Nachrichten oder `source=compact` bedeuten keinen neuen/verlorenen Chat (`docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:213-214`). | Der Lifecycle-Grund ist bekannt, aber nicht als Identitätsinvariante modelliert; Hook-Payload-`source` wird nicht persistiert und ein abweichender `SessionStart` wäre weiterhin blind bindbar (`WhisperM8/Services/AgentChats/ClaudeHookEventStore.swift:36-46`, `WhisperM8/Services/AgentChats/AgentSessionStatusCoordinator.swift:345-363`). | Für `compact` denselben ProviderSessionKey erzwingen, nur Kontext-/Transcript-Metadaten aktualisieren und Rohhistorie, aktive Kette sowie UI-Zusammenfassung getrennt behandeln (`docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:208-214`). |
| **Gleichzeitiges Resume derselben Session** | Pro lokaler UUID verhindert die Registry einen zweiten Controller, aber zwei Workspace-Zeilen können dieselbe externe ID tragen; Hook-Binding und Indexer-Fallback prüfen keine Belegung durch andere Rows (`WhisperM8/Views/AgentTerminalView.swift:341-364`, `WhisperM8/Services/AgentChats/AgentSessionStatusCoordinator.swift:345-363`, `WhisperM8/Services/AgentChats/AgentSessionStore.swift:600-649`). | Zwei Writer auf denselben `ProviderSessionKey(provider, canonicalConfigRoot, externalSessionID)` interleaven dieselbe Session; pro Key darf deshalb nur ein Writer laufen oder der zweite Start muss bewusst forken (`docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:214-216`, `docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:263-264`). | Prozess-Exklusivität ist nur pro lokaler Chat-ID, nicht pro gescoptem Provider-Key definiert (`WhisperM8/Views/AgentTerminalView.swift:329-364`). | Atomare Writer-Lease auf `ProviderSessionKey` mit `launchID` als Besitzer; bei belegtem Key Start blockieren und „Forken“ anbieten. Indexer/Hook-Binding müssen belegte IDs ebenfalls ausschließen (`docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:250-267`). |
| **Crash/Kill während Session** | Erstellung, Launchmarker und Binding werden sofort geflusht; der Controller/PID ist jedoch flüchtig, und bei einem Crash zwischen Fork-Spawn und korrekter Child-Bindung bleibt die Zeile `nil` oder bereits falsch am Parent. Ein späterer Repair arbeitet nur mit Projekt-/Zeitheuristik (`WhisperM8/Services/AgentChats/AgentSessionStore.swift:513-566`, `WhisperM8/Views/AgentSessionDetailView.swift:633-650`, `WhisperM8/Services/AgentChats/AgentSessionStatusCoordinator.swift:345-363`, `WhisperM8/Services/AgentChats/AgentSessionStore.swift:1189-1218`). | Prozessende ist keine Sessionlöschung; abgeschlossene Records und der letzte gute Indexstand bleiben erhalten, ein unvollständiger Tail ist temporäre negative Evidenz (`docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:213-215`, `docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:224-239`). | Kein persistenter Übergang/keine Prozessgeneration erlaubt die eindeutige Recovery von „Fork gestartet, Child noch nicht committed“ (`WhisperM8/Models/AgentChat.swift:225-303`). | Launch-Transition vor Spawn persistieren; nach Neustart per Config-Root, Parent, Launchzeit, Transcriptpfad und eindeutiger neuer JSONL reconciliieren. Negative Scans markieren `recoveryRequired` und löschen/überschreiben kein Binding (`docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:250-271`). |
| **Projektordner verschoben/umbenannt** | `AgentProject.path` ist die cwd-Quelle; fehlt der Pfad, verweigert der Builder den Launch. Der Locator sucht bei Pfad-Miss per Session-ID, akzeptiert den Fund aber nur, wenn der erste gelesene JSONL-cwd dem erwarteten aktuellen Pfad entspricht; der Indexer kann aus dem alten Transcript-cwd zusätzlich ein separates Projekt erzeugen oder eine bestehende ID dorthin umhängen (`WhisperM8/Models/AgentChat.swift:142-200`, `WhisperM8/Services/AgentChats/AgentCommandBuilder.swift:149-165`, `WhisperM8/Services/AgentChats/AgentSessionTranscript.swift:343-419`, `WhisperM8/Services/AgentChats/AgentSessionStore.swift:788-823`). | Encoded cwd ist nur Discovery-Hinweis; Config-Root, Session-ID, autoritativer Transcriptpfad und tatsächliches cwd bleiben getrennte Werte. Ein Move ist ein expliziter Relink-/Migrationsfall, kein Sessionverlust (`docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:214-220`). | Workspace-Projektpfad und Claude-Discoverypfad sind faktisch gekoppelt; es gibt keinen Relink, keine Pfadhistorie und keinen persistierten autoritativen `transcript_path` (`WhisperM8/Models/AgentChat.swift:142-200`, `WhisperM8/Models/AgentChat.swift:225-303`). | Projekt-Relink aktualisiert die Workspace-Referenz unabhängig vom ProviderSessionKey; Binding behält Config-Root/ID/Transcriptpfad, prüft alle tatsächlichen cwd-Records und zeigt eine bewusste Migration an, ohne externe Claude-Dateien still zu verändern (`docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:214-220`, `docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:264-267`). |

## 4. Feature-Regressions-Hinweis

Der Umbau ist ein Identitäts-Refactor und darf folgende bestehende Eigenschaften nicht verlieren (`docs/audit/2026-07-agent-chats-deep-dive/06-umsetzung/README.md:112-149`):

- **Capability-Zustand statt absoluter ID-Vergabe.** `hostAssignedUnsupported` bewahrt Weg B: Fresh und Fork starten ohne vorab erzwungene Ziel-ID. `hostAssignedVerified` darf vorab reservierte IDs ausschließlich nach vollständig bestandener Live-Probe des konkreten CLI-Binaries verwenden; ein Mismatch fällt fail-closed in Recovery und deaktiviert die Verifikation (`WhisperM8/Services/AgentChats/AgentCommandBuilder.swift:355-360`, `WhisperM8/Views/AgentChatsView+SessionLifecycle.swift:41-46`).
- **Explizites Resume und sichtbarer Verlust statt stillem Fresh-Start.** Ein bestehender Chat resumiert per exakter bestätigter ID; fehlt das Transcript, bleibt das Binding erhalten und der Launch stoppt mit verständlicher Fehlermeldung (`WhisperM8/Views/AgentSessionDetailView.swift:537-610`, `WhisperM8/Views/AgentSessionDetailView.swift:755-769`).
- **Account-Isolation bleibt Teil der Identität.** Fork erbt den Account der Quelle, Resume läuft unter dem realen Transcript-Root, und Main-/Profil-Roots dürfen nicht vermischt werden (`WhisperM8/Views/AgentChatsView+SessionLifecycle.swift:99-103`, `WhisperM8/Services/AgentChats/AgentCommandBuilder.swift:244-250`, `WhisperM8/Services/AgentChats/AgentCommandBuilder.swift:362-405`).
- **Stabile lokale UI-Identität.** Tabs, Fenster, Grid-Slots, Titel, Farbe, Archiv und Auswahl bleiben an der lokalen Chat-UUID hängen; ein Claude-Branch-Wechsel darf keinen neuen UI-Tab erzwingen, ein expliziter Fork dagegen behält den heutigen separaten Tab (`WhisperM8/Services/AgentChats/AgentWindowStore.swift:105-183`, `WhisperM8/Views/AgentChatsView+SessionLifecycle.swift:78-112`).
- **Hook-first mit konservativem Fallback.** Hooks bleiben die schnellste Live-Quelle und der Indexer bleibt Recovery-/Discovery-Fallback; der Fallback muss aber Mehrdeutigkeit, Account-Scope und bereits belegte IDs respektieren (`WhisperM8/Services/AgentChats/ClaudeHookBridge.swift:104-181`, `WhisperM8/Views/AgentSessionDetailView.swift:676-747`, `docs/audit/2026-07-agent-chats-deep-dive/01-subsysteme/hooks-accounts.md:121-126`).
- **Status- und Compact-Semantik bleiben erhalten.** Hook-Evidenz behält Vorrang vor Transcript-Status, ESC-Abbruch bleibt als Transcript-Ausnahme erhalten, und `clear`/`resume`/`compact` beenden nicht irrtümlich den PTY-Prozess (`WhisperM8/Services/AgentChats/AgentSessionStatusCoordinator.swift:63-69`, `WhisperM8/Services/AgentChats/AgentSessionStatusCoordinator.swift:228-253`, `WhisperM8/Services/AgentChats/AgentSessionStateMachine.swift:222-235`).
- **Crash-sichere Strukturmutationen bleiben synchron gesichert.** Chat-Erstellung, Launchmarker und bestätigtes Binding werden weiterhin sofort geflusht; neu hinzu kommt nur, dass Fork-Transition und Child-Binding ebenfalls atomar und crash-sicher persistiert werden (`WhisperM8/Services/AgentChats/AgentSessionStore.swift:513-566`, `WhisperM8/Views/AgentSessionDetailView.swift:633-650`, `WhisperM8/Services/AgentChats/AgentSessionStatusCoordinator.swift:345-363`).
- **Parent und Child bleiben beide auffindbar.** Forken verändert den Eltern-Chat nicht, erbt weiterhin dessen Darstellungskontext und darf Auto-Naming, Transcript-Anzeige oder späteres Resume beider Zweige nicht zusammenführen (`WhisperM8/Views/AgentChatsView+SessionLifecycle.swift:78-112`, `docs/audit/2026-07-agent-chats-deep-dive/03-vergleich/workflow3-kandidaten.md:208-212`).

Die Abnahme des Umbaus muss daher nicht nur „Fork resumiert Child“ prüfen, sondern auch Fresh-Start, normales Resume, Hook-Ausfall mit eindeutiger Indexer-Recovery, Account-Profil, `/compact`, parallelen Writer-Block, Crash zwischen `spawned` und `committed` sowie Projekt-Relink als getrennte Verhaltens-Oracles festhalten (`docs/audit/2026-07-agent-chats-deep-dive/06-umsetzung/README.md:112-149`).
